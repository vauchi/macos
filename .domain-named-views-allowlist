# SPDX-FileCopyrightText: 2026 Mattia Egloff <mattia.egloff@pm.me>
# SPDX-License-Identifier: GPL-3.0-or-later
#
# Allow-list for macos/Vauchi/Views/*.swift consumed by
# scripts/scripts/check-domain-named-views.sh (wired into `just lint macos`).
#
# Ratchet-down: each retirement MR (problem record
# 2026-05-02-macos-humble-ui-retirement, G1) deletes both the view
# file and its entry below. The destination is the platform-required
# subset only.
#
# One filename per line. Lines starting with `#` and blank lines
# ignored.
#
# --- Platform-required (likely permanent) ----------------------------
# LockScreenView — pre-unlock screen; runs before core is decrypted,
#                  so cannot render a core ScreenModel.
LockScreenView.swift

# --- To-be-retired (G1 targets) --------------------------------------
# All sheets retired 2026-05-04 after the file-picker
# `ExchangeCommand` shipped (Phase 2A/2B/3 of
# `2026-05-03-core-file-picker-command`):
# - DeviceLinkSheet retired 2026-05-03 — inlined CoreSheetView at
#   the VauchiApp call site.
# - ImportContactsSheet retired 2026-05-04 in `tidy/retire-import-
#   contacts-sheet` — menu rewired through MoreEngine's
#   `import_contacts` action.
# - ImportBackupSheet retired 2026-05-04 (this MR) — orphan-
#   confirmed: post-Phase-2B no caller emits StartBackupImport on
#   core; the variant remains in the enum as a chrome hint
#   (handled as `case ... break` in AppViewModel + CoreSceneView)
#   and Phase 4 retires it.
# G1 closed for macOS.
